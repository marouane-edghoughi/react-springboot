import './Navbar.css';

function Navbar() {
    return(
        <nav className="sticky-top navbar">
            <span className="logo">Students Manager</span>
        </nav>
    );
}

export default Navbar;